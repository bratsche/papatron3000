# Papatron3000

**Papa challenge**

## Installation

`mix deps.get`
`mix ecto.create`
`mix ecto.migrate`
`mix escript.build`

At this point, either explore the CLI app using `./papatron3000` or
run the test suite using `mix test`.
